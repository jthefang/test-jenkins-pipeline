pipeline {
    agent any
    
    stages {
        stage("init") {
            echo "initing!"
        }
        stage("build") {
            echo "buidling"
        }
        stage("test") {
            echo "testing"
        }
        stage("deploy") {
            echo "deploying"
        }
    }   
}